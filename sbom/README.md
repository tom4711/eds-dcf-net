# SBOM (Software Bill of Materials)

An SBOM is generated automatically for every release and published as GitHub Release assets alongside the NuGet packages.

## Formats

| File | Format | Tool |
|---|---|---|
| `bom.cdx.json` | [CycloneDX JSON](https://cyclonedx.org/) | [CycloneDX for .NET](https://github.com/CycloneDX/cyclonedx-dotnet) |
| `sbom.spdx.json` | [SPDX JSON 2.3](https://spdx.dev/) | Derived from the CycloneDX BOM ([`cyclonedx-to-spdx.jq`](cyclonedx-to-spdx.jq)) |

## Where to find them

Each [GitHub Release](https://github.com/dborgards/eds-dcf-net/releases) includes the SBOM files as downloadable assets:

```
EdsDcfNet.<version>.nupkg
EdsDcfNet.<version>.snupkg
bom.cdx.json
sbom.spdx.json
```

## How they are generated

SBOM generation is fully automated as part of the Semantic Release workflow:

1. **CycloneDX BOM** — generated by `dotnet CycloneDX` directly from `src/EdsDcfNet/EdsDcfNet.csproj` after the NuGet package is packed, then pruned (see below).
2. **SPDX BOM** — derived from the pruned CycloneDX BOM via [`cyclonedx-to-spdx.jq`](cyclonedx-to-spdx.jq), so both formats describe the same published artifact.
3. Both files are attached to the GitHub Release as downloadable assets.

The CycloneDX tool version is pinned in [`.config/dotnet-tools.json`](../.config/dotnet-tools.json) and kept up to date by Renovate.

### Root-component metadata

The `dotnet CycloneDX` CLI reads only the *version* from the project file; it does not populate the root component's author, supplier, license, description, or external references. Those fields are supplied by the metadata template [`bom-metadata.template.xml`](bom-metadata.template.xml) and merged in via `--import-metadata-path`. The component name, version, and type are still overridden on the CLI (`--set-name`, `--set-version`, `--set-type`), and the `purl`/`bom-ref` are generated by `--set-nuget-purl`.

Keep the template in sync with `src/EdsDcfNet/EdsDcfNet.csproj` (authors, license expression, description) when those values change.

### Dependency-graph pruning and completeness

`dotnet CycloneDX --exclude-dev` removes packages flagged as development dependencies (analyzers, SourceLink, Polyfill) but leaves their transitive dependencies behind as orphan components no longer reachable from the root. Because the published package declares zero runtime dependencies, those orphans would misrepresent the supply chain.

After generation the BOM is post-processed with [`prune-cyclonedx.jq`](prune-cyclonedx.jq), which keeps only components reachable from the root component, drops dangling dependency entries, and adds a `compositions` block asserting the dependency inventory is `complete`. The filter uses no hardcoded package names, so genuine runtime dependencies added later are retained automatically.

### SPDX derived from CycloneDX

The SPDX document is generated from the pruned `bom.cdx.json` by [`cyclonedx-to-spdx.jq`](cyclonedx-to-spdx.jq): the root component and every remaining component become SPDX packages (carrying name, version, supplier, license, `downloadLocation`, and a `purl` external reference), and the CycloneDX dependency graph becomes `DESCRIBES`/`DEPENDS_ON` relationships. Timestamp and document namespace are derived from the CycloneDX BOM, so the output is deterministic.

This replaces the previous approach of fetching the [GitHub Dependency Graph SBOM API](https://docs.github.com/en/rest/dependency-graph/sboms), which described the whole repository's default-branch graph (build, test, and Node-based release tooling) rather than the published package, used a non-release `versionInfo`, and carried no supplier/license metadata. Deriving from CycloneDX keeps both formats consistent and artifact-accurate.

## Triage release-tooling advisories

Dependency alerts can target runtime packages, test packages, GitHub Actions, or the Node-based release tooling used by `semantic-release`. Triage the alert before changing package versions:

1. Identify the dependency path in the alert or lockfile. For example, a vulnerable package may be bundled under `@semantic-release/npm` → `npm` → `node_modules/npm/node_modules/...` rather than referenced by the library itself.
2. Classify the blast radius. Runtime dependencies affect consumers; packages with `PrivateAssets="all"`, test-only packages, GitHub Actions, and `semantic-release` dependencies affect build or release automation.
3. Prefer the smallest version movement that refreshes the vulnerable subtree. For bundled `npm` dependencies, this may require re-resolving the locked `npm` package instead of editing the nested package directly.
4. Verify the local dependency state with the relevant package manager, for example `npm audit` for release tooling alerts.
5. For release-impacting changes, confirm the next GitHub Release contains the expected `bom.cdx.json` and `sbom.spdx.json` assets.

When an advisory only affects release tooling, the fix should still be reviewed as supply-chain work because those tools publish the package and release artifacts.

## Authoritative source

The **CycloneDX BOM** (`bom.cdx.json`) is the authoritative SBOM: it is generated directly from the released project file, pruned to the package's true runtime footprint, and carries full root-component metadata. The **SPDX BOM** (`sbom.spdx.json`) is derived from it for consumers that require SPDX, so both formats describe the same published artifact.
